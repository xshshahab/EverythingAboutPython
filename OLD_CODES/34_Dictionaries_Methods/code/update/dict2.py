# Create a dictionary
dict2 = {"x": 10, "y": 20}

# Use the update() method to overwrite the value of an existing key
dict2.update({"x": 30})

# Result: dict2 now contains {"x": 30, "y": 20}
print(dict2)
