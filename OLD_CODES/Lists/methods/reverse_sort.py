# Define a list called 'num' with several integer values
num = [1, 4, 4, 12, 43, 33, 7, 78, 6, 77, 9]

# Print the original list of numbers
print(num)

# Sort the list in descending order by using the 'reverse=True' argument
num.sort(reverse=True)

# Print the sorted list after sorting in descending order
print(num)
